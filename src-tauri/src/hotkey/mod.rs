// TODO(milestone B+): user-configurable hotkeys (e.g., open settings, mute).
// The A.1 safety-net Cmd+Shift+H / Ctrl+Shift+H is registered directly in
// `lib.rs::run()` rather than here, since it must exist before any feature
// hotkey work.
